# Build artifacts (regenerated by Scripts/build-kiosk-apk.ps1 → gradlew)
build/
.gradle/
local.properties

# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# Keystore (lives in parent project's Tools/)
*.keystore

# Local secrets baked into BuildConfig (HA token etc.) -- the published APK contains them but
# the source tree on GitHub does not. Each build environment provides its own.
secrets.properties
